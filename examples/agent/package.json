{
  "name": "stellar-allowance-agent-example",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "description": "A buying agent with an allowance. Paste the file from the Stellar Allowance user tab into agent.mjs and run it.",
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env agent.mjs"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^16.2.0"
  }
}
